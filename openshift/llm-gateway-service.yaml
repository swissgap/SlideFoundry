apiVersion: v1
kind: Service
metadata:
  name: presentation-factory-llm-gateway
  labels:
    app.kubernetes.io/name: presentation-factory
    app.kubernetes.io/component: llm-gateway
spec:
  selector:
    app.kubernetes.io/name: presentation-factory
    app.kubernetes.io/component: llm-gateway
  ports:
    - name: http
      port: 8081
      targetPort: http
